# Arc speed scripts

| Script | Purpose |
|--------|---------|
| `build-vulkan.ps1` | Release Vulkan build with `GGML_NATIVE=ON` |
| `bench.ps1` | `llama-bench` A/B (Xe2 FA vs `-LegacyFa`) |
| `run-server.ps1` | Server preset: `-fa on -ngl 99 -b 512 -ub 256` |

See [docs/arc-speed.md](../../docs/arc-speed.md).
